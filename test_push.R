library(dplyr)
library(ggplot2)
install.packages("git")
gdfd # this will trigger a conflict for sure it should at least 

This is additional changes i made 
there cannot be a conflict with a push as the local branch is "ahead" of the gitbranch 